<%= form_for(@admin_high_score) do |f| %>
  <% if @admin_high_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_high_score.errors.count, "error") %> prohibited this admin_high_score from being saved:</h2>

      <ul>
      <% @admin_high_score.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game %><br />
    <%= f.text_field :game %>
  </div>
  <div class="field">
    <%= f.label :score %><br />
    <%= f.number_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
